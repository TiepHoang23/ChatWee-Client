const MessageItem = () => {
    return (
        <div className="bg-orange-400 h-fit max-w-sm p-2 rounded-2xl mb-3">
            This is a message from user to user is a message from user to user is a message from user to user
        </div>
    );
}

export default MessageItem;